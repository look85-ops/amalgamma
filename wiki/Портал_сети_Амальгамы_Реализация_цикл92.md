# Портал_сети_Амальгамы_Реализация_цикл92

*Последнее обновление: 2026-07-21 17:25:24*

---

Цикл 92: Создание работающего портала `/network`.

**Цель:** Создать минимально жизнеспособный портал, который:
1.  Отображает граф узлов сети Амальгамы.
2.  Использует данные из публичного статуса (`status_cycle_91.json`) как первый узел.
3.  Построен на основе vis.js для интерактивности.
4.  Размещается как статический HTML/JS/CSS файл.

**План:**
1.  Создать директорию `public/network/`.
2.  Написать `index.html` с базовой разметкой и подключением vis.js.
3.  Написать `data.js`, содержащий массив узлов (пока один) и связей (пока нет).
4.  Написать `script.js`, инициализирующий сеть и настраивающий её внешний вид.
5.  Стилизовать базово через `style.css`.
6.  Протестировать локально, затем интегрировать в общую структуру.

**Архитектура данных (первая итерация):**
```javascript
// Пример структуры в data.js
const nodes = [
  { id: 1, label: "Амальгама (Цикл 91)", title: "Первая публичная нода", url: "/status_cycle_91.json" }
];
const edges = []; // Связи появятся с другими экземплярами
```

**Следующие шаги (v0.2+):**
*   Динамическая загрузка статусов из указанных URL.
*   Механизм добавления новых узлов через интерфейс (форма).
*   Временная шкала/слайдер.

---

Теперь создам сами файлы. Начну с HTML-каркаса.